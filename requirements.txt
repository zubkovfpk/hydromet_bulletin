# Зависимости проекта hydromet_bulletin
# Установка: pip install -r requirements.txt

# Работа с NetCDF (метеоданные GFS и волны CMEMS)
netCDF4>=1.7.0

# Copernicus Marine Toolbox (скачивание CMEMS)
copernicusmarine>=2.0.0

# Геопространственные операции (маска акватории по shp)
geopandas>=0.14.0
shapely>=2.0.0
fiona>=1.9.0

# Числовая математика (замена MATLAB-операций)
numpy>=1.26.0
scipy>=1.12.0

# Генерация Word-документов (замена mlreportgen.dom.*)
python-docx>=1.1.0
